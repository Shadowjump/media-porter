<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notice"
        Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="#0B0E13"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13.5">

  <Window.Resources>
    <SolidColorBrush x:Key="Text"     Color="#E9EEF6"/>
    <SolidColorBrush x:Key="Muted"    Color="#8B98AB"/>
    <SolidColorBrush x:Key="Line"     Color="#212A38"/>
    <SolidColorBrush x:Key="LineSoft" Color="#19212C"/>

    <LinearGradientBrush x:Key="AccentGrad" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#FF3B5C" Offset="0"/>
      <GradientStop Color="#FF8A3D" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="AccentButton" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="Padding" Value="22,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" CornerRadius="10" Background="{StaticResource AccentGrad}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GhostButton" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="Padding" Value="18,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" CornerRadius="10" Background="#00FFFFFF"
                    BorderBrush="{StaticResource Line}" BorderThickness="1">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#141C27"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="Switch" TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource Muted}"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12.5"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" Background="#00FFFFFF">
              <Border x:Name="track" Width="34" Height="20" CornerRadius="10"
                      Background="#1B2431" BorderBrush="{StaticResource Line}" BorderThickness="1"
                      VerticalAlignment="Center">
                <Ellipse x:Name="knob" Width="13" Height="13" Fill="#7B8AA0"
                         HorizontalAlignment="Left" Margin="3,0,0,0"/>
              </Border>
              <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="track" Property="Background" Value="{StaticResource AccentGrad}"/>
                <Setter TargetName="track" Property="BorderBrush" Value="#00000000"/>
                <Setter TargetName="knob" Property="Fill" Value="White"/>
                <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right"/>
                <Setter TargetName="knob" Property="Margin" Value="0,0,3,0"/>
                <Setter Property="Foreground" Value="{StaticResource Text}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Background="#0B0E13" BorderBrush="#1C2532" BorderThickness="1" CornerRadius="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- header -->
      <Grid Grid.Row="0" x:Name="NoticeTitleBar" Background="#0B0E13" Height="52">
        <StackPanel Orientation="Horizontal" Margin="22,0,0,0" VerticalAlignment="Center">
          <Ellipse x:Name="NoticeDot" Width="10" Height="10" Fill="#FBBF24" Margin="0,0,12,0"/>
          <TextBlock x:Name="NoticeTitle" Text="Notice" Foreground="{StaticResource Text}"
                     FontWeight="SemiBold" FontSize="14.5" VerticalAlignment="Center"/>
        </StackPanel>
      </Grid>

      <StackPanel Grid.Row="1" Margin="22,0,22,20">
        <TextBlock x:Name="NoticeBody" Foreground="{StaticResource Muted}" FontSize="13"
                   TextWrapping="Wrap" LineHeight="20"/>
      </StackPanel>

      <Border Grid.Row="2" Background="#0E1218" BorderBrush="{StaticResource LineSoft}"
              BorderThickness="0,1,0,0" CornerRadius="0,0,9,9" Padding="22,14">
        <Grid>
          <CheckBox x:Name="NoticeDontAsk" Style="{StaticResource Switch}"
                    Content="Don't check again on startup"
                    VerticalAlignment="Center" HorizontalAlignment="Left"/>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="NoticeAlt" Style="{StaticResource GhostButton}" Content="Continue anyway"
                    Margin="0,0,9,0"/>
            <Button x:Name="NoticeOk" Style="{StaticResource AccentButton}" Content="Open iTunes"/>
          </StackPanel>
        </Grid>
      </Border>
    </Grid>
  </Border>
</Window>
