<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose your device"
        Width="640" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="#0B0E13"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13.5">

  <Window.Resources>
    <SolidColorBrush x:Key="Text"     Color="#E9EEF6"/>
    <SolidColorBrush x:Key="Muted"    Color="#8B98AB"/>
    <SolidColorBrush x:Key="Faint"    Color="#5E6B80"/>
    <SolidColorBrush x:Key="Line"     Color="#212A38"/>
    <SolidColorBrush x:Key="LineSoft" Color="#19212C"/>

    <LinearGradientBrush x:Key="AccentGrad" StartPoint="0,0" EndPoint="1,1">
      <GradientStop Color="#FF3B5C" Offset="0"/>
      <GradientStop Color="#FF8A3D" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="AccentGradSoft" StartPoint="0,0" EndPoint="1,0" Opacity="0.18">
      <GradientStop Color="#FF3B5C" Offset="0"/>
      <GradientStop Color="#FF8A3D" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="AccentButton" TargetType="Button">
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="Padding" Value="22,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" CornerRadius="10" Background="{StaticResource AccentGrad}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="bd" Property="Opacity" Value="0.8"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="bd" Property="Opacity" Value="0.35"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GhostButton" TargetType="Button">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="Height" Value="38"/>
      <Setter Property="Padding" Value="18,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" CornerRadius="10" Background="#00FFFFFF"
                    BorderBrush="{StaticResource Line}" BorderThickness="1">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#141C27"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="Switch" TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource Muted}"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12.5"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" Background="#00FFFFFF">
              <Border x:Name="track" Width="34" Height="20" CornerRadius="10"
                      Background="#1B2431" BorderBrush="{StaticResource Line}" BorderThickness="1"
                      VerticalAlignment="Center">
                <Ellipse x:Name="knob" Width="13" Height="13" Fill="#7B8AA0"
                         HorizontalAlignment="Left" Margin="3,0,0,0"/>
              </Border>
              <ContentPresenter Margin="10,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="track" Property="Background" Value="{StaticResource AccentGrad}"/>
                <Setter TargetName="track" Property="BorderBrush" Value="#00000000"/>
                <Setter TargetName="knob" Property="Fill" Value="White"/>
                <Setter TargetName="knob" Property="HorizontalAlignment" Value="Right"/>
                <Setter TargetName="knob" Property="Margin" Value="0,0,3,0"/>
                <Setter Property="Foreground" Value="{StaticResource Text}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ScrollThumb" TargetType="Thumb">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Thumb">
            <Border Background="#2A3646" CornerRadius="4" Margin="3,0"/>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ScrollBar">
      <Setter Property="Width" Value="11"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ScrollBar">
            <Grid Background="#00FFFFFF">
              <Track x:Name="PART_Track" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Focusable="False"/>
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Style="{StaticResource ScrollThumb}"/>
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Focusable="False"/>
                </Track.IncreaseRepeatButton>
              </Track>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- one device row -->
    <Style x:Key="DeviceRow" TargetType="ListBoxItem">
      <Setter Property="Foreground" Value="{StaticResource Text}"/>
      <Setter Property="Padding" Value="14,11"/>
      <Setter Property="Margin" Value="0,2"/>
      <Setter Property="IsEnabled" Value="{Binding Selectable}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ListBoxItem">
            <Border x:Name="bd" Background="#111721" CornerRadius="11"
                    BorderBrush="{StaticResource LineSoft}" BorderThickness="1"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#161E2A"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="bd" Property="Background" Value="{StaticResource AccentGradSoft}"/>
                <Setter TargetName="bd" Property="BorderBrush" Value="#FF6B4D"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="bd" Property="Background" Value="#0E141C"/>
                <Setter TargetName="bd" Property="Opacity" Value="0.75"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <DataTemplate x:Key="DeviceTemplate">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Name}" Foreground="#E9EEF6" FontSize="13.5" FontWeight="SemiBold"/>
            <Border Background="#16202C" CornerRadius="5" Padding="6,1" Margin="9,0,0,0"
                    VerticalAlignment="Center">
              <TextBlock Text="{Binding Years}" Foreground="#5E6B80" FontSize="10.5"/>
            </Border>
          </StackPanel>
          <TextBlock Text="{Binding Note}" Foreground="#8B98AB" FontSize="11.5" Margin="0,4,10,0"
                     TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
          <TextBlock Text="{Binding Encode}" Foreground="#E9EEF6" FontSize="12" TextAlignment="Right"/>
          <TextBlock Text="{Binding Detail}" Foreground="#5E6B80" FontSize="11" TextAlignment="Right"
                     Margin="0,3,0,0"/>
        </StackPanel>
      </Grid>
    </DataTemplate>
  </Window.Resources>

  <Border Background="#0B0E13" BorderBrush="#1C2532" BorderThickness="1" CornerRadius="10">
    <Grid Margin="0">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- header -->
      <Grid Grid.Row="0" x:Name="PickerTitleBar" Background="#0B0E13" Height="52">
        <StackPanel Orientation="Horizontal" Margin="22,0,0,0" VerticalAlignment="Center">
          <Grid Width="18" Height="18" Margin="0,0,11,0">
            <Ellipse Fill="{StaticResource AccentGrad}"/>
            <Ellipse Width="6" Height="6" Fill="#0B0E13"/>
          </Grid>
          <TextBlock Text="Choose your device" Foreground="{StaticResource Text}"
                     FontWeight="SemiBold" FontSize="14.5" VerticalAlignment="Center"/>
        </StackPanel>
      </Grid>

      <TextBlock Grid.Row="1" Margin="22,0,22,14" Foreground="{StaticResource Muted}" FontSize="12.5"
                 TextWrapping="Wrap"
                 Text="Video is encoded for whichever device you pick here, and the choice is remembered. Music is not affected - AAC in .m4a plays on every one of them, including the ones greyed out at the bottom that have no video at all."/>

      <!-- list -->
      <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="14,0,8,0" Padding="8,0">
        <ItemsControl x:Name="FamilyList">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <StackPanel Margin="0,0,0,14">
                <TextBlock Text="{Binding Family}" Foreground="#5E6B80" FontSize="11"
                           FontWeight="SemiBold" Margin="4,0,0,7"/>
                <ListBox ItemsSource="{Binding Items}"
                         Background="#00FFFFFF" BorderThickness="0"
                         ItemContainerStyle="{StaticResource DeviceRow}"
                         ItemTemplate="{StaticResource DeviceTemplate}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"/>
              </StackPanel>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>

      <!-- footer -->
      <Border Grid.Row="3" Background="#0E1218" BorderBrush="{StaticResource LineSoft}"
              BorderThickness="0,1,0,0" CornerRadius="0,0,9,9" Padding="22,14">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <CheckBox x:Name="ChkHevc" Grid.Row="0" Style="{StaticResource Switch}" Margin="0,0,0,14"
                    Content="Use HEVC on devices that support it - about half the file size"/>

          <Grid Grid.Row="1">
            <TextBlock x:Name="PickerSummary" Foreground="{StaticResource Faint}" FontSize="11.5"
                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,262,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
              <Button x:Name="BtnPickCancel" Style="{StaticResource GhostButton}" Content="Cancel"
                      Margin="0,0,9,0"/>
              <Button x:Name="BtnPickOk" Style="{StaticResource AccentButton}" Content="Use this device"/>
            </StackPanel>
          </Grid>
        </Grid>
      </Border>
    </Grid>
  </Border>
</Window>
